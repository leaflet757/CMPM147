/**
 * @author Kate Compton
 */

define(["common"], function(common) {'use strict';
    var branch = new Class({
        init : function(tree, parent) {

        }
    });

    var lTree = new Class({

    });

});
